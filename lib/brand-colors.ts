/**
 * Brand colors for UI and browser chrome (theme-color meta, PWA manifest).
 * Keep --primary / --secondary in globals.css aligned with these values.
 */
export const BRAND_PRIMARY = "#ff0000";
export const BRAND_SECONDARY = "#ff5900";

/** Address bar / status bar color on mobile Chrome, Safari, and installed PWAs */
export const BRAND_THEME_COLOR = BRAND_PRIMARY;

/** Splash / standalone app background behind the UI */
export const BRAND_BACKGROUND_COLOR = "#0a0a0a";
